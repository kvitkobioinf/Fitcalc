<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.fitcalc.fitcalc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".posilki" />
        <activity android:name=".Aktywnosc" />
        <activity android:name=".wybor_posilku" />
        <activity android:name=".obliczenie" />
        <activity android:name=".CEL" />
        <activity android:name=".Rejestracjaglowna" />
        <activity android:name=".Logowanie" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>